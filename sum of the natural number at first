#include <stdio.h>
#include <string.h>
int main()
{
    int n,s=0;
    printf("enter the number :");
    scanf("%d",&n);
    for (int i=1;i<=n;i++)
    {
        s+=i;
    }
    printf("sum of the natural number :%d",s);
    
    return 0;
    
}
