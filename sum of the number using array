#include <stdio.h>
int main()
{
    int n, Sum=0;
printf ("enter the size of array \n");
scanf ("%d",&n);
int a[n];
for (int i=0; i<n; i++)
{
    printf ("a[%d]:", i);
    scanf("%d",&a[i]);
    Sum=Sum+a[i];
}
printf ("\n sum of the array = %d", Sum);
return 0;

    
}
