#include <stdio.h>
#include <string.h>
void main()
{
    int x,y,z;
    printf("the number of passrnger=\n");
    scanf("%d",&x);
    printf("the numer of row =\n");
    scanf("%d",&y);
    z=x/y;
    printf("the number seats in one = %d",z);
    
}
